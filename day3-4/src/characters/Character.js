import { Game } from '../Game.js';

export class Character {
    constructor(name, hp, dmg, mana, status) {
      this.name = name;
      this.hp = hp;
      this.dmg = dmg;
      this.mana = mana
      this.status = "playing"
    }

    takeDamage(dmg){

    }

    dealDamage(victim){

    }

  
    
  }
